mutation Register($registerInput: RegisterInput!) {
	register(registerInput: $registerInput) {
		code
		success
		message
		errors {
			message
			field
		}
		user {
			username
			email
			updatedAt
			createdAt
		}
	}
}
